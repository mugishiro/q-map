{
  "name": "q-map",
  "version": "0.1.0",
  "description": "QMap stack",
  "license": "MIT",
  "scripts": {
    "test": "vitest run backend/test",
    "build:lambda": "tsc -p backend/tsconfig.json"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.129",
    "@types/node": "^22.10.1",
    "typescript": "^5.6.3",
    "vitest": "^1.3.1"
  },
  "dependencies": {
    "aws-sdk": "^2.1692.0"
  }
}
